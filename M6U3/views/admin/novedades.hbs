<section class="holder">
    <h2>{{title}}</h2>
    <div class="flex-center">
        <div class="card shadow p-3 mb-5 bg-body rounded text-center h-100">
            <div class="card-body">
                <div class="header">
                    <a type="button p-5" class="btn btn-primary" href="/admin/novedades/add">Nuevo <i class="fa-solid fa-plus"></i></a>
                </div>
                <div class="body">
                    <table class="table table-dark table-striped">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Título</td>
                                <th scope="col">Subtitulo</td>
                                <th scope="col">Cuerpo</td>
                                <th scope="col">Acción</td>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each novedades}}
                                <tr>
                                    <th class="table-light text-truncate" scope="row"><p class="d-inline-block text-truncate" style="max-width: 30rem;">{{this.id}}</p></td>
                                    <td class="table-light text-truncate"><p class="d-inline-block text-truncate" style="max-width: 30rem;">{{this.title}}</p></td>
                                    <td class="table-light text-truncate"><p class="d-inline-block text-truncate" style="max-width: 30rem;">{{this.subtitle}}</p></td>
                                    <td class="table-light text-truncate"><p class="d-inline-block text-truncate" style="max-width: 30rem;">{{this.body}}</p></td>
                                    <td class="table-light actions">
                                        <form method="PUT" action="/admin/novedades/{{this.id}}/update">
                                            <button type="submitt button p-5" class="btn btn-primary"><i class="fas fa-pencil"></i></button>
                                        </form>
                                        <form method="GET" action="/admin/novedades/{{this.id}}/delete">
                                            <button type="submitt button p-5" class="btn btn-primary"><i class="fas fa-trash"></i></button>
                                        </form>
                                    </td>
                                </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>